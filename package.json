{
    "name": "Nosflare",
    "version": "6.0.2",
    "main": "worker.js",
    "scripts": {
      "build:relay": "esbuild src/relay-worker.js --bundle --outfile=worker.js --platform=neutral --target=es2020 --format=esm --allow-overwrite",
      "build": "npm run build:relay"
    },
    "dependencies": {
      "@noble/curves": "^1.0.0",
      "esbuild": "^0.12.1"
    }
  }